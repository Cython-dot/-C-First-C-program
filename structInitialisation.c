// Full initialisation

struct date dateOne = {20, 8, 2020}; 

// Like an array initialisation, fewer values might be contained in the structure

struct date dateOne = {20, 8};

// sets dateOne.day to 20 and dateOne.month to 8 but gives no initial value to date.year
                        
                              
